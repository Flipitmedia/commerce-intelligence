# Commerce Intelligence — Environment Variables
# Copia este archivo como .env y completa los valores

# Server
HOST=0.0.0.0
PORT=8000
DEBUG=true

# Database (SQLite, ruta relativa al root del proyecto)
DATABASE_PATH=data/ci.db
